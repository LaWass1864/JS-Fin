// Avec le TS on va pouvoir faire aussi du JS
// On ne peut pas mettre un boolean sur un number
let chiffre = 42;

const sayName = (name?: string) => {
    document.body.innerHTML = "Bonjour" + name;
}

sayName("wass");

